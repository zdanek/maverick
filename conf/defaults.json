// These are fall-back defaults.  They can/should be over-ridden higher up the hierarchy.
{
	// NB: Some defaults are set in localconf.json to protect potentially sensitive data

	// Set blank defaults for host/ip values.  This is to prevent parsing errors
	"hostname": "",
	"fqdn": "",
	"primaryip": "",
	"hostaliases": "",
	
	// List of all internal IP addresses.  These IPs are used for default iptables and other access control,
	//  so be careful what is put in here.
	// These should be IPs, not CIDR ranges, as some logic won't handle CIDR syntax.
	// If all_ips is left blank, ALL IPs will be allowed for configured rules.
	"all_ips": [
	],

	"classes" : [
		"base",
		"maverick-desktop",
		"maverick-security"
	],
	
	"base::users::mav_sudopass": false,
	"maverick-desktop::enable": false,
	
	// "maverick-network::dnsclient": "disabled",
    // "maverick-network::ntpclient": "enabled",
    // "maverick-network::ethernet": true,
    // "maverick-network::wireless": true,
    // "maverick-network::netman": false,
    // "maverick-network::predictable": false,
    // "maverick-network::eth0_name": "eth0",
    // "maverick-network::eth0_mode": "dhcp",
    // "maverick-network::wlan0_name": "wlan0",
    // "maverick-network::wlan0_mode": "dhcp",
    // "maverick-network::wlan0_ssid": false,
    // "maverick-network::wlan0_psk": false,
    // "maverick-network::wlan1_name": "wlan1",
    // "maverick-network::wlan1_mode": "dhcp",
    // "maverick-network::wlan1_ssid": false,
    // "maverick-network::wlan1_psk": false,

	"maverick-security::fail2ban": false,
	"maverick-security::firewall": true

}
