---
:backends:
  - json
:json:
  :datadir: conf/ 
:hierarchy:
  - localconf
  - "%{environment}/nodes/%{::hostname}"
  - "%{environment}/defaults"
  - "local-nodes/%{::hostname}"
  - "sample-nodes/%{::hostname}"
  - maverick-defaults
  - puppet-defaults
