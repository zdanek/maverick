// These are local config settings.  Because they can include sensitive information like usernames and passwords, this file
//  is excluded from ever being committed back to git.
{
	// Set passwords
	// "base::users::mav_password": "", // Set mav user encrypted password here (obtain from /etc/shadow)
	// "base::users::root_password": "", // Set root user encrypted password here (obtain from /etc/shadow)
	
	// Set defaults for wifi.  If set, any wifi interface detected will be automatically activated with these settings.
	"wifi_ssid": 		"",
	"wifi_passtype": 	"wpa2",
	"wifi_passphrase":	false, // unencrypted wifi passphrase, recommended to use psk below instead
	"wifi_psk": 		"", // must be encrypted psk created using 'wpa_passphrase myssid mypassphrase'

	// Set defaults for git.  These don't need to be set but if they are will be used.
	"git_username": 	"",
	"git_email":		"",
	
	// "maverick-network::ethernet::eth0_mode": "static",
	// "maverick-network::ethernet::eth0_address": "192.168.1.50",
	// "maverick-network::ethernet::eth0_netmask": "255.255.255.0",
	// "maverick-network::ethernet::eth0_gateway": "192.168.1.254",
	// "maverick-network::wireless::wlan0_mode": "static",
	// "maverick-network::wireless::wlan0_address": "192.168.1.51",
	// "maverick-network::wireless::wlan0_netmask": "255.255.255.0",
	// "maverick-network::wireless::wlan0_gateway": "192.168.1.254",
	// "maverick-network::wireless::wlan0_ssid": "",
	// "maverick-network::wireless::wlan0_psk": "", // must be encrypted psk created using 'wpa_passphrase myssid mypassphrase'.  Overrides wifi_psk above for specific interface

	// "maverick-desktop::enable": true,

	// "maverick-baremetal::raspberry::init::gpumem": 256,
	// "maverick-baremetal::raspberry::init::overclock": "High",
	// "maverick-baremetal::raspberry::init::spi": true, // required for lcd screens
	// "maverick-baremetal::raspberry::init::serialconsole": true,
	
	// Add classes here
	"classes":		[
		// "maverick-network"
	]
	
}
