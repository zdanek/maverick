# URL prefix
app_prefix = 'maverick-api/'

# Secret Key - used to set secure cookie.  KEEP SECRET
app_secretkey = 'super_s3cret'

# Disable SSL - Used to avoid loading ssl_keyfile and ssl_certfile - Not safe for deployment
disable_ssl = False

# SSL keyfile - Path to valid ssl keyfile (not used if disable_ssl is set to True)
ssl_keyfile = "/srv/maverick/data/security/ssl/web/mavweb.key"

# SSL certfile - Path to valid ssl certfile (not used if disable_ssl is set to True)
ssl_certfile = "/srv/maverick/data/security/ssl/web/mavweb.crt"

# Base Directory, defaults to directory of maverick_api.py script if not set or None
# basedir = None

# Database backend - used to select the database backend [tinydb, mongo, sqlite]
database_backend = "tinydb"

# Data Directory
datadir = '/srv/maverick/data/web/api/<%= @instance %>/'

# Turn on development options - Not safe for deployment
development = <% if @devmode == true %>True<% else %>False<% end %>

# Turn on debug mode
debug = <% if @debug == true %>True<% else %>False<% end %>

# Log Directory
logdir = '/srv/maverick/var/log/web/api/<%= @instance %>/'

# Interface to listen on: 0.0.0.0 represents all interfaces
server_interface = '0.0.0.0'

# Port to listen on
server_port = <%= @apiport %>

# List of modules to allow loading. Takes precedence over deny list
module_allow_list = []

# List of modules to deny loading
module_deny_list = ["mavros"]
